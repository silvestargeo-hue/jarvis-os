node_modules
.next
out
.env
.env*.local

# convex/_generated is committed: it is derived only from the local convex/
# function definitions (not from any deployment), so CI and fresh clones can
# typecheck and build without a Convex login. Regenerated by `convex dev`.

# OS
.DS_Store
Thumbs.db

# WebLLM model cache lives in IndexedDB/CacheStorage, nothing to ignore on disk.
.vercel
